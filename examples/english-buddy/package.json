{
  "name": "english-buddy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Development app for voxpal createAgent",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests for english-buddy'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@types/qrcode-terminal": "^0.12.2",
    "dotenv": "^17.3.1",
    "qrcode-terminal": "^0.12.0",
    "zod": "^4.1.11",
    "zupa": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
