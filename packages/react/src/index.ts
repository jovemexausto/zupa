/**
 * @zupa/react — React SDK for Zupa agents.
 *
 * Provides hooks (useAgentState, useAgentChat) and
 * WebSocket connection management for reactive frontends.
 * Depends only on @zupa/core types — no server code.
 */
export { };
